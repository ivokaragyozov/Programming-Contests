#include<iostream>
#include<cstdio>
using namespace std;
struct marsh
{
    int b, e, cost;
};
marsh road[5005];
int main()
{
    int n, a, b;
    scanf("%d %d %d", &n, &a, &b);
    int r[5005];
    for(int i=0; i<n; i++)
    {
        scanf("%d %d %d", &road[i].b, &road[i].e, &road[i].cost);
        for(int j=s; j<e; j++)
        {
            r[j]=i;
        }
    }
    int sum=0;
    for(int i=a; i<=b; i++)
    {

    }
}
