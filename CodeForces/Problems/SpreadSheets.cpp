#include <iostream>
#define endl '\n'
using namespace std;

int n;
string coordinates;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    cin>>n;
    for(int i=0; i<n; i++)
    {
        cin>>coordinates;
        if(coordinates[1] >= 'A' && coordinates[1] <= 'Z')
        {

        }
    }
}
