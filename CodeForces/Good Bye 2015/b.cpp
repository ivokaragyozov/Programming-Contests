#include <bits/stdc++.h>
#define endl '\n'
using namespace std;

long long a, b;
string s;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin>>a>>b;

    while(a > 0)
    {
        s = (char)(a % 2 + '0') + 2;
        a /= 2;
    }

    for(long long )
}
