#include <iostream>
#define endl '\n'
using namespace std;

const int maxN = 1000;

int n, a[maxN + 5], b[maxN + 5], c[maxN + 5];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin>>n;
    for(int i = 0; i < n; i++)
    {
        cin>>a[i]>>b[i]>>c[i];
    }

    for(int i = 0; i < n; i++)
    {
        for(int j = i + 1; j < n; j++)
        {

        }
    }
}
