#include <bits/stdc++.h>
#define endl '\n'
using namespace std;

int n, a, q;
priority_queue<int> pq;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin>>n>>q;
    for(int i = 0; i < n; i++)
    {
        cin>>a;
        pq.push(a);
    }

    for(int i = 0; i < q; i++)
    {
        cin>>
    }
}
